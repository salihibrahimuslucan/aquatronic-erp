{
  "name": "aquatronic-erp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:uretim": "vite build --mode uretim && node tools/build-uretim.mjs",
    "build:all": "vite build && npm run build:uretim",
    "preview": "vite preview",
    "crm-api": "python tools/crm_api.py"
  },
  "devDependencies": {
    "vite": "^6.3.5",
    "vite-plugin-singlefile": "^2.3.3"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.110.0"
  }
}
